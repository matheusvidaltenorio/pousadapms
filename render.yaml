# Render Blueprint - Pousada PMS
# https://docs.render.com/blueprint-spec

databases:
  - name: pousada-db
    databaseName: pousada_pms
    plan: free

services:
  # API NestJS
  - type: web
    name: pousada-api
    runtime: node
    region: oregon

    buildCommand: npm install && npx prisma generate --schema=apps/api/prisma/schema.prisma && npx prisma migrate deploy --schema=apps/api/prisma/schema.prisma && npm run build:api
    startCommand: node apps/api/dist/src/main.js

    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: pousada-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
      - key: CORS_ORIGIN
        value: https://pousadapms-web.onrender.com

  # Frontend React (Static Site) - static sites não suportam region
  - type: web
    name: pousada-web
    runtime: static
    buildCommand: npm install && npm run build:web
    staticPublishPath: apps/web/dist

    # SPA: roteamento client-side (React Router)
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

    envVars:
      - key: VITE_API_URL
        value: https://pousada-api.onrender.com/api
